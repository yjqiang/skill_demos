---
name: xhs-explore
argument-hint: "[可选：内容偏好，如"好物安利""观点分享""穿搭"]"
description: |
  浏览小红书推荐流、查看笔记详情和评论。
  当用户想看推荐内容、刷首页、查看某条笔记的详情/评论、
  或有 feed_id 想获取完整内容时使用。
---

## 输入判断

- 用户想浏览推荐 → 步骤 1
- 用户提供了 feed_id → 步骤 2
- 用户提到了内容偏好（如"找好物推荐""看穿搭"）→ 步骤 1，同时在浏览时主动标注匹配项

## 执行流程

### 1. 获取推荐流

调用 `list_feeds`（无参数），返回首页推荐笔记列表。

展示每条笔记的标题、作者、互动数据（点赞/收藏/评论数），附带 `feed_id` 和 `xsec_token`——这两个是后续查看详情的必要凭证，必须保留。

如果用户提到了内容偏好（如"好物安利""穿搭""观点分享""痛点解决"等），在列出结果时：
- 根据笔记标题和简介判断是否匹配用户偏好
- 对匹配的笔记标注 ⭐ 推荐，帮助用户快速定位
- 如果用户没提偏好，展示所有结果即可，不需要做额外筛选

### 2. 查看笔记详情

调用 `get_feed_detail`：
- `feed_id`（string，必填）— 从浏览结果中获取
- `xsec_token`（string，必填）— 同上，二者缺一不可
- `load_all_comments`（bool，可选，默认 false 仅返回前 10 条）
- `limit`（int，可选，load_all_comments=true 时生效，默认 20）
- `click_more_replies`（bool，可选，是否展开二级回复）
- `reply_limit`（int，可选，跳过回复数超过此值的评论，默认 10）
- `scroll_speed`（string，可选：slow | normal | fast）

展示：笔记内容、图片、作者信息、互动数据、评论列表。


## 失败处理

| 场景 | 处理 |
|---|---|
| 未登录 | 引导使用 [xhs-login](../xhs-login/SKILL.md) 子技能 |
| 笔记已删除或不可见 | 告知用户该笔记无法访问 |
| feed_id 无效 | 确认参数来源是否正确（是否从 list_feeds 结果中取得） |
